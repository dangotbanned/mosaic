{
  "name": "spec-python",
  "private": true,
  "scripts": {
    "check": "uv run ruff check",
    "fmt": "uv run ruff format",
    "generate": "uv run --module scripts.schema_mod && uv run datamodel-codegen --profile=spec && pnpm run lint && uv run --module scripts.export_all && pnpm run lint",
    "lint": "uv run ruff check && uv run ruff format"
  },
  "version": "0.29.2"
}