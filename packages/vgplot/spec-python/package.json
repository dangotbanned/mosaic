{
  "name": "spec-python",
  "private": true,
  "scripts": {
    "generate": "uv run scripts/schema_mod.py && uv run datamodel-codegen && uv run ruff check && uv run ruff format"
  },
  "version": "0.29.2"
}